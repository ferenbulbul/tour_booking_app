import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF007BFF); // Mavi ton
  static const Color secondary = Color(0xFF6C757D);
  static const Color grey = Color.fromARGB(209, 136, 132, 132);
  static const Color background = Color(0xFFF8F9FA); // ✅ EKLEDİK
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF6C757D);
  static const Color white = Colors.white;
  static const Color error = Colors.red; // ✅ error da lazım!
}
