import 'dart:ui';

class AppColors {
  // Background surfaces
  static const background = Color(0xFFF9FAFB); // soft white
  static const surface = Color(0xFFFFFFFF); // card white

  // Brand Colors (premium travel blue)
  static const primary = Color(0xFF2563EB);
  static const primaryDark = Color(0xFF1E40AF);
  static const secondary = Color(0xFFFACC15);

  // Text Colors
  static const textPrimary = Color(0xFF1F2937); // dark gray
  static const textSecondary = Color(0xFF6B7280); // gray
  static const textLight = Color(0xFF9CA3AF); // light gray

  // States
  static const success = Color(0xFF22C55E);
  static const danger = Color(0xFFEF4444);
}
